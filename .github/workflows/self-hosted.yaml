name: Self Hosted
on:
  push:
    branches:
      - repo_mgmt_and_github_actions-self
jobs:
  build:
    runs-on: self-hosted
    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Build image
        working-directory: ./provision/bsc/deploy_chain
        run: |
             sudo docker-compose up -d
