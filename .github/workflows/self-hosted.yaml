name: Self Hosted
on:
  push:
    # paths:
    #   - ''
jobs:
  build:
    runs-on: self-hosted
    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Build image
        working-directory: ./provision/bsc/deploy_chain
        run: |
             sudo docker-compose up -d
